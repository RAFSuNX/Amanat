# kubectl create secret generic amanat-secrets \
#   --from-env-file=.env.production \
#   --namespace=amanat --dry-run=client -o yaml > k8s/secret.yaml
#
# OR apply directly:
#   kubectl create secret generic amanat-secrets \
#     --from-env-file=.env.production \
#     --namespace=amanat

# This file is a template — never store real values here.
apiVersion: v1
kind: Secret
metadata:
  name: amanat-secrets
  namespace: amanat
type: Opaque
stringData:
  DATABASE_URL: "postgresql://amanat:CHANGE@postgres:5432/amanat"
  AUDIT_DATABASE_URL: "postgresql://amanat_audit:CHANGE@postgres-audit:5432/amanat_audit"
  REDIS_URL: "redis://redis:6379"
  BETTER_AUTH_SECRET: "CHANGE"
  BETTER_AUTH_URL: "https://amanat.rafsunx.com"
  R2_ENDPOINT: "https://966a7d2874704e203a507de3b9b50f4f.r2.cloudflarestorage.com"
  R2_ACCESS_KEY_ID: "CHANGE"
  R2_SECRET_ACCESS_KEY: "CHANGE"
  R2_BUCKET_NAME: "amanat"
  R2_PUBLIC_URL: "https://pub-a6bb19b164f2483cae62bc32df512e6e.r2.dev"
  RESEND_API_KEY: "CHANGE"
  RESEND_FROM_EMAIL: "Amanat <noreply@rafsunx.com>"
